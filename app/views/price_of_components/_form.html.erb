<%= form_for(@price_of_component) do |f| %>
  <% if @price_of_component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price_of_component.errors.count, "error") %> prohibited this price_of_component from being saved:</h2>

      <ul>
      <% @price_of_component.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :price_range_id %><br>
    <%= f.number_field :price_range_id %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :component %><br>
    <%= f.text_field :component %>
  </div>
  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
