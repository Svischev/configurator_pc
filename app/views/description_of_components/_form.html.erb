<%= form_for(@description_of_component) do |f| %>
  <% if @description_of_component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@description_of_component.errors.count, "error") %> prohibited this description_of_component from being saved:</h2>

      <ul>
      <% @description_of_component.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
	  <div class="field">
    <%= f.label :rus_name %><br>
    <%= f.text_field :rus_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :full_description %><br>
    <%= f.text_area :full_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
